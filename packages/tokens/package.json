{
  "name": "@svx/tokens",
  "version": "0.1.0",
  "description": "Two-tier (primitive -> semantic) design tokens, built to CSS, JSON, Kotlin (Jetpack Compose), and Swift.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/json/tokens.json",
  "files": [
    "dist",
    "tokens"
  ],
  "scripts": {
    "build": "node build-tokens.mjs",
    "clean": "rimraf dist",
    "lint": "echo \"no lint configured for tokens\" && exit 0",
    "typecheck": "echo \"no typecheck configured for tokens\" && exit 0"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "style-dictionary": "^4.3.0"
  }
}
