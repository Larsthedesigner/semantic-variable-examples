{
  "name": "@svx/tokens",
  "version": "0.1.0",
  "description": "Two-tier (primitive -> semantic) design tokens authored as CSS custom properties, built to a portable CSS bundle, JSON, Kotlin (Jetpack Compose), and Swift.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/json/tokens.json",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "node build-tokens.mjs",
    "clean": "rimraf dist",
    "lint": "echo \"no lint configured for tokens\" && exit 0",
    "typecheck": "echo \"no typecheck configured for tokens\" && exit 0"
  },
  "devDependencies": {
    "postcss": "^8.4.49",
    "rimraf": "^6.0.1"
  }
}
